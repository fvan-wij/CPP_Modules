#pragma once

#include <vector>

class PmergeMe
{
	public:
		PmergeMe();
		~PmergeMe();

		std::vector<int>	sort(std::vector<int>&	elements);

	private:
		// std::vector<int>						_intVec;
		// std::size_t								_leftover;
};
